// Answering Question 1 

// a.
 const customerAges = [];
 customerAges.push('3');
 customerAges.push('9');
 customerAges.push('23');
 customerAges.push('64');
 customerAges.push('2');
 customerAges.push('8');
 customerAges.push('28');
 customerAges.push('93');
 
 const firstArray = [0];
  console.log (0); 


 //b.
 const lastArray = [customerAges.length - 1];
 console.log(lastArray);
  customerAges.push("25")
 
 //c
  for (let i = 0; i < customerAges.length; i++) {
      console.log (customerAges[i]);
   }


 //d
   const average = customerAges => customerAges.reduce((a,b) => a + b ) / customerAges.length;
   console.log(average([3,9,23,64,2,8,28,93]));
 
 

// Answering question 3,4 from the second assignment 
//a
const names = ("Sam","Tommy","Tim","Sally","Buck",
"Bob")

const names2 = names2.concat ("Sam","Tommy","Tim","Sally","Buck",
"Bob")

for (var i = 0; i < str.length; i++) {
    alert(str.charAt(i));
    console.log(str.charAt(i));
}
//b
for ( i = 0; i < NamesOfPeople.length(); i++)
    if (Character.isLetter(NamesOfPeople.charAt(i)))


 var AverageList = names2 => names2.reduce((a,b) => a + b ) / names2.length;
console.log(AverageList["Sam","Tommy","Tim","Sally","Buck",
"Bob"]);
  
for (let i = 0; i < NameOfPeople.length; i++) {
  console.log (NameOfPeople[i]);
}

// question 3 and 4 

//I can access the last element by subtracting the length of the array by 1. 
//And I can access the first array by inputing (0) as the index it starts from
 //0 to the amount of variables in that array.

//I have been having an issue with changing my 



// Answering question 3 

//1
namesArray = ["Kelly", "Sam", "Kate"]
nameLengths = [5, 3, 4]

//2
const nameLengths = [namesLengths.length];
for(let a = 0; a < namesLengths.length; a++){
    nameLengths[a] = namesLengths[a].length();
}
System.out.println(Arrays.toString(nameLengths));
nameLengths = [5, 3, 4]

const averages = nameLengths => nameLengths.reduce((a,b) => a + b ) / nameLengths.length;
  console.log(averages([5,3,4]));


//3
const words = ("hello")
const numbers = ("2")

let results = words.concat("hello" + numbers) 
  console.log("hello,hello")


//4
function createFullName(firstName,lastName) {
     console.log(firstName + ' ' + lastName)
}

createFullName('Jasmine','Godoy');

//5
const array1 = (1,2,3)
const array2 = (4,5,6)

function listOfNumbers(array1,array2) {
  return (listOfNumbers > 100)
    returns('true')

}
//6 
const Averages = listOfNumbers => listOfNumbers.reduce((a,b) => a + b ) / listOfNames.length;
   console.log(Averages([1,2,3,4,5,6]));

  


//7 
array1 = (2,30,50)
array2 =(40,30,10)

const sum = arrays => arrays.reduce((a,b) => a + b ) / listOfNames.length;
   console.log(sum([2,30,50,40,30,10]));

function sum(array1 + array2) => ( array1 > array2 ) return ('true'))

//8
const willBuyDrink = ('isHotOutside')
const moneyInPocket = ('20')

function willBuyDrink = ('isHotOutside' + ' ' + 'moneyInPocket') {
  if ('isHotOutside and moneyInPocket > 10.50')
   return ('true') 
}

//9
   const flavorsOfCandy = (raspberry,vanilla,chocolate)
const namesOfKids = (smith,jarey,carl)

   function namesofKids = (a,b,c,) => flavorsofCandy + (a,b,c,) 

 // I want each kid in my function to get candy returned to them.Smith gets raspberry, jarey gets vanilla and carl get chocolate.






